module BlogPosts exposing (BlogIndexItem, blogIndex)


type alias BlogIndexItem =
    { title : String
    , date : Int -- an epoch
    , permalink : String
    }


reverseDateComparator a b =
    case compare a.date b.date of
        LT ->
            GT

        GT ->
            LT

        EQ ->
            EQ


blogIndex : List BlogIndexItem
blogIndex =
    List.sortWith reverseDateComparator <|
--@here
  ]
